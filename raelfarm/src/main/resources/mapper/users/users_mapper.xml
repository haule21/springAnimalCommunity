<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mtbatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="haule.raelfarm.mapper.UsersMapper">
	<select id="idCheck" parameterType="String" resultType="integer">
		SELECT 
			IFNULL((SELECT 1 FROM DUAL WHERE EXISTS (SELECT 1 FROM USERS_TEST WHERE USERID = #{param0} )), 0) 
		FROM 
			DUAL
		
	</select>
</mapper>